import soma from './meuModulo.mjs'

soma(10, 1)
soma(1, 1)
soma(3, 9)

// Aula ensinando a utilização do import e export no node JS
// Essa funcionalidade veio com o ES6
// Não é tão utilizada quanto o module.export e o require
// Lembre que quando se trabalha com o import e export o arquivo é .mjs